const home = "/";

//db rest api

//CURD
const create = "/create";
const read = "/read";
const update = "/update";
const remove = "/delete";

// model
const category = "/category";
const event = "/event";
const image = "/image";
const like = "/like";
const scrap = "/scrap";
const shop = "/shop";
const user = "/user";

const routes = {
  home,
  create,
  read,
  update,
  remove,
  category,
  event,
  image,
  like,
  scrap,
  shop,
  user,
};

export default routes;
